# 🧠 Erros que já cometi

- Confundi `<a>` com `<link>`.

- Achei que `<br>` fechava com `</br>`.

# ❌ Erros que já cometi

- Confundi `href` com `src`.
- Confundi `.` (class) com `#` (id).
- Escrevi `.body` em vez de `body`.
- Misturei `border` com `border-radius`.
- Achei que `padding` empurrava os elementos de fora (na verdade aumenta o espaço interno).
- Achei que `margin` servia apenas para empurrar para os lados (na verdade cria espaço externo ao redor do elemento).

# Erros que cometi

- Confundi <head> com <header>.

Como lembrar:

- <head> = informações/configurações da página (o usuário normalmente não vê).
- <header> = cabeçalho visível da página (logo, título, menu, pesquisa...).